const fetch = require('node-fetch');

it("should get a login token", asyn({userName, password})=>{


    let loginToken="";//this is not what it will be eventually

expect.(loginToken.length).toBe(36);//check for a valid token
}
);